# Haskell build artefacts
*.o
*.hi
*.dyn_o
*.dyn_hi

# Cabal/Stack output
dist/
dist-newstyle/
.stack-work/
.ghc.environment.*

# Lean / Lake build artefacts
**/.lake/
**/build/

# Julia build / coverage artefacts
# Project.toml + Manifest.toml ARE committed (lockfile discipline);
# only build/coverage byproducts are ignored.
*.jl.cov
*.jl.*.cov
*.jl.mem
**/deps/build.log
**/deps/build.log.txt
**/deps/deps.jl
**/deps/usr/
LocalPreferences.toml

# LaTeX build artefacts (if any paper-shaped artefact lands later)
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fdb_latexmk
*.fls
*.bbl
*.blg

# macOS metadata
.DS_Store

# Project-internal governance — kept local, not tracked publicly
CLAUDE.md

# paper-index hook artifacts (local navigation aids; not project content)
.paper-index.json
**/.paper-index.json

# Project-internal governance / process records — kept local, not tracked publicly
LAVALAMP_SPEC.md
artifact_registry.md
changelog.md
docs/
dashboard.md

# Swift build artifacts (lavalamp_menubar; users build locally)
src/swift/lavalamp_menubar/lavalamp_menubar
src/swift/**/*.dSYM/
